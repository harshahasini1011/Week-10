#include <stdio.h>

int main() {
    int arr[] = {10, 20, 30, 40};
    int *p = arr;

    printf("Value at p = %d\n", *p);
    printf("Value at p+1 = %d\n", *(p+1));
    printf("Value at p+2 = %d\n", *(p+2));

    return 0;
}
